<li id="<%= feeduser_item.id %>">
  
  <span class="user">
    User: <%= feeduser_item.name %>
  </span>

  <% if can? :delete, feeduser_item %>
  <%= link_to "delete", feeduser_item, method: :delete, data: { confirm: "You sure?" }, title: feed_user.name %>
  <% end %>
</li>